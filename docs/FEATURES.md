# Feature documentation

Generated by `scripts/write_feature_docs.py` from `bankcanary.features.registry`; do not
edit by hand. Every column of `features_v1` (prototype P1) and `features_v2` (P1 + P2) is
listed below in build order within its group.

## What CAMELS is

Bank supervisors in the United States grade every bank on six components, known by the
acronym CAMELS: **C**apital adequacy (the cushion that absorbs losses), **A**sset quality
(how many loans are going bad), **M**anagement (the quality of the decisions being made,
visible in data mostly as growth and expansion choices), **E**arnings (whether the bank
makes money), **L**iquidity (whether it can pay depositors who ask for their money) and
**S**ensitivity to market risk (how much interest-rate moves change the value of what it
holds). The feature groups below follow that structure and add loan concentration, trends
over time, the bank's structure and the macroeconomic backdrop of its home state.

## How to read the tables

- **formula** uses the FDIC field codes of the Call Report (`config/fields.yaml`) and,
  for derived features, the names of other registered features. A `_q` suffix marks an
  income item de-accumulated to one quarter; "annualised" multiplies it by four.
- **unit**: `ratio` is a plain fraction (0.05 = 5 %), `percent` is as published by the
  FDIC, `delta_*` is the change of that unit, `flag` is True / False, `count` is a
  number of quarters.
- **sign** is the monotone direction the constrained gradient-boosting model may enforce:
  `+1` when failure risk rises with the value, `-1` when it falls, `0` when no direction
  is assumed. The same sign is written into every explanation in the registry.
- **proto** is the prototype that introduced the feature (P1 columns form `features_v1`).

83 registered features: 43 P1 (`features_v1`) and 40 P2 (`features_v2` = P1 + P2, plus the `ytd_prev_missing` flag in both tables).

## Capital adequacy (C)

6 features.

| name | formula | unit | sign | proto | explanation |
|---|---|---|---|---|---|
| `equity_to_assets` | `eq / asset` | ratio | -1 | P1 | Share of the balance sheet funded by shareholders; the cushion that absorbs losses before depositors are hit. |
| `tier1_leverage` | `rbc1aaj` | percent | -1 | P1 | Regulatory Tier 1 leverage ratio; below 4% a bank is undercapitalised and below 2% it is critically undercapitalised. |
| `total_rbc_ratio` | `rbcrwaj` | percent | -1 | P1 | Total risk-based capital over risk-weighted assets; missing for banks that elect the community bank leverage ratio from 2020. |
| `total_rbc_ratio_missing` | `rbcrwaj is missing` | flag | 0 | P1 | True when the risk-based ratio is not reported, so the model can tell a CBLR filer from a genuinely low ratio. |
| `tangible_equity_to_assets` | `(eq - intan) / (asset - intan)` | ratio | -1 | P1 | Equity after stripping goodwill and other intangibles, which are worthless in a resolution. |
| `adjusted_tier1_leverage` | `(rbct1j + min(0, (scaf - scaa) + (schf - scha))) / asset * 100` | percent | -1 | P2 | Tier 1 leverage recomputed after deducting unrealised AFS and HTM losses (gains are not added); the denominator is period-end total assets rather than the regulatory quarterly average, so it differs slightly from the published ratio even with no losses. |

## Asset quality (A)

7 features.

| name | formula | unit | sign | proto | explanation |
|---|---|---|---|---|---|
| `noncurrent_ratio` | `nclnls / lnlsgr` | ratio | +1 | P1 | Share of loans 90+ days past due or on nonaccrual; the most direct read on credit losses already in the pipeline. |
| `npa_to_assets` | `(nclnls + ore) / asset` | ratio | +1 | P1 | Non-performing assets, including foreclosed property, relative to the whole balance sheet. |
| `early_delinquency` | `p3asset / lnlsgr` | ratio | +1 | P1 | Assets 30-89 days past due over gross loans; an early signal before loans turn noncurrent. |
| `nco_rate` | `annualised ntlnls_q / average lnlsgr` | ratio | +1 | P1 | Annualised net charge-offs as a share of average loans; the losses actually realised this quarter. |
| `reserve_coverage` | `lnatres / nclnls, capped at 10` | ratio | -1 | P1 | Loan-loss allowance per dollar of noncurrent loans; low coverage means future losses will hit earnings and capital. |
| `texas_ratio` | `(nclnls + ore) / (eq - intan + lnatres), capped at 10` | ratio | +1 | P1 | Problem assets over tangible equity plus reserves; values near or above 1 have historically preceded most failures. |
| `texas_ratio_capped` | `texas_ratio hit the cap` | flag | +1 | P1 | True when tangible capital plus reserves was zero or negative (or the ratio exceeded 10), so the reported value is the cap rather than a computed ratio. |

## Management (M)

3 features.

| name | formula | unit | sign | proto | explanation |
|---|---|---|---|---|---|
| `asset_growth_4q` | `log(asset / asset 4 quarters earlier)` | log_ratio | 0 | P1 | One-year balance-sheet growth; rapid growth is a classic management red flag, but shrinkage can signal run-off, so both tails carry risk. |
| `asset_growth_12q` | `log(asset / asset 12 quarters earlier)` | log_ratio | 0 | P1 | Three-year balance-sheet growth, capturing a sustained expansion strategy; both tails carry risk. |
| `loan_growth_4q` | `log(lnlsgr / lnlsgr 4 quarters earlier)` | log_ratio | 0 | P1 | One-year loan-book growth; fast lending growth often means loosened underwriting, while a shrinking book can mean distress. |

## Earnings (E)

4 features.

| name | formula | unit | sign | proto | explanation |
|---|---|---|---|---|---|
| `roa_q` | `annualised netinc_q / average asset` | ratio | -1 | P1 | Annualised quarterly return on average assets; the core measure of profitability. |
| `nim_q` | `annualised (intinc_q - eintexp_q) / average ernast (else asset)` | ratio | 0 | P1 | Annualised net interest margin over average earning assets; the spread the bank earns on its lending. |
| `efficiency_ratio` | `nonix_q / (nim_q + nonii_q)` | ratio | +1 | P1 | Noninterest expense per dollar of revenue; higher means a costlier operation. |
| `provision_rate` | `annualised elnatr_q / average lnlsgr` | ratio | +1 | P1 | Annualised loan-loss provisions as a share of average loans; what management expects to lose. |

## Liquidity (L)

7 features.

| name | formula | unit | sign | proto | explanation |
|---|---|---|---|---|---|
| `brokered_share` | `bro / dep` | ratio | +1 | P1 | Share of deposits bought through brokers; hot money that leaves first when a bank weakens. |
| `loans_to_deposits` | `lnlsnet / dep` | ratio | +1 | P1 | How far lending is funded by deposits; above 1 the bank relies on borrowed funds. |
| `liquid_assets_ratio` | `(chbal + frepo + sc) / asset` | ratio | -1 | P1 | Cash, fed funds sold and securities as a share of assets; what can be sold or pledged quickly. |
| `wholesale_funding_ratio` | `(othbor + bro) / asset` | ratio | +1 | P1 | Borrowed money (Federal Home Loan Bank advances included) plus brokered deposits over assets; funding that reprices or runs under stress. |
| `uninsured_share` | `depunins / dep` | ratio | +1 | P2 | Estimated uninsured deposits as a share of total deposits; the money that runs first because it is not protected in a failure. |
| `uninsured_to_liquid_assets` | `depunins / (chbal + frepo + scaf)` | ratio | +1 | P2 | Uninsured deposits per dollar of cash, fed funds sold and available-for-sale securities at fair value; above 1 a full run cannot be met without selling the held-to-maturity book. |
| `large_time_deposit_share` | `(cd3les + cd3t12 + cd1t3 + cdov3) / dep` | ratio | +1 | P2 | Time deposits of $250,000 or more as a share of total deposits; rate-sensitive money that is typically uninsured and leaves at maturity. |

## Sensitivity to market risk (S)

5 features.

| name | formula | unit | sign | proto | explanation |
|---|---|---|---|---|---|
| `afs_unrealized_to_tier1` | `(scaf - scaa) / rbct1j` | ratio | -1 | P2 | Unrealised gain (positive) or loss (negative) on available-for-sale securities relative to Tier 1 capital; already in book equity but a marker of rate exposure. |
| `htm_unrealized_to_tier1` | `(schf - scha) / rbct1j` | ratio | -1 | P2 | Unrealised gain or loss on held-to-maturity securities relative to Tier 1 capital; not booked anywhere, so a large negative value is hidden capital erosion. |
| `unrealized_loss_to_tier1` | `((scaf - scaa) + (schf - scha)) / rbct1j` | ratio | -1 | P2 | Total unrealised securities gain or loss over Tier 1 capital; negative means a loss and a value near -1 means the losses would wipe out Tier 1 if realised. |
| `securities_to_assets` | `sc / asset` | ratio | 0 | P2 | Securities as a share of total assets; the part of the balance sheet whose value moves with market rates. |
| `htm_share_of_securities` | `scha / sc` | ratio | +1 | P2 | Held-to-maturity share of the securities book; HTM losses are unbooked and the bonds cannot be sold without tainting the whole portfolio. |

## Loan concentration

9 features.

| name | formula | unit | sign | proto | explanation |
|---|---|---|---|---|---|
| `construction_to_capital` | `lnrecons / rbc (fallback rbct1j + lnatres)` | ratio | +1 | P1 | Construction and land loans relative to total risk-based capital; above 1 is the regulators' concentration threshold. |
| `cre_to_capital` | `(lnrecons + lnremult + lnrenrot, else lnrenres) / rbc (fallback rbct1j + lnatres)` | ratio | +1 | P1 | Commercial real estate exposure relative to capital; above 3 is the supervisory concentration threshold. |
| `share_construction` | `lnrecons / lnlsgr` | ratio | +1 | P1 | Construction and land development loans as a share of the loan book. |
| `share_nonfarm_nonres` | `lnrenres / lnlsgr` | ratio | +1 | P1 | Nonfarm nonresidential (commercial property) loans as a share of the loan book. |
| `share_multifamily` | `lnremult / lnlsgr` | ratio | 0 | P1 | Multifamily residential loans as a share of the loan book. |
| `share_residential` | `lnreres / lnlsgr` | ratio | 0 | P1 | 1-4 family residential mortgages as a share of the loan book. |
| `share_ci` | `lnci / lnlsgr` | ratio | 0 | P1 | Commercial and industrial loans as a share of the loan book. |
| `share_consumer` | `lncon / lnlsgr` | ratio | 0 | P1 | Consumer loans as a share of the loan book. |
| `share_agri` | `lnag / lnlsgr` | ratio | 0 | P1 | Agricultural production loans as a share of the loan book. |

## Trends and persistence

19 features.

| name | formula | unit | sign | proto | explanation |
|---|---|---|---|---|---|
| `d1q_noncurrent_ratio` | `noncurrent_ratio - noncurrent_ratio 1 quarter-end earlier (same cert)` | delta_ratio | +1 | P2 | Change in noncurrent_ratio over the last 1 quarter; NaN when the earlier quarter-end was not reported. |
| `d4q_noncurrent_ratio` | `noncurrent_ratio - noncurrent_ratio 4 quarter-ends earlier (same cert)` | delta_ratio | +1 | P2 | Change in noncurrent_ratio over the last 4 quarters; NaN when the earlier quarter-end was not reported. |
| `d1q_texas_ratio` | `texas_ratio - texas_ratio 1 quarter-end earlier (same cert)` | delta_ratio | +1 | P2 | Change in texas_ratio over the last 1 quarter; NaN when the earlier quarter-end was not reported. |
| `d4q_texas_ratio` | `texas_ratio - texas_ratio 4 quarter-ends earlier (same cert)` | delta_ratio | +1 | P2 | Change in texas_ratio over the last 4 quarters; NaN when the earlier quarter-end was not reported. |
| `d1q_roa_q` | `roa_q - roa_q 1 quarter-end earlier (same cert)` | delta_ratio | -1 | P2 | Change in roa_q over the last 1 quarter; NaN when the earlier quarter-end was not reported. |
| `d4q_roa_q` | `roa_q - roa_q 4 quarter-ends earlier (same cert)` | delta_ratio | -1 | P2 | Change in roa_q over the last 4 quarters; NaN when the earlier quarter-end was not reported. |
| `d1q_equity_to_assets` | `equity_to_assets - equity_to_assets 1 quarter-end earlier (same cert)` | delta_ratio | -1 | P2 | Change in equity_to_assets over the last 1 quarter; NaN when the earlier quarter-end was not reported. |
| `d4q_equity_to_assets` | `equity_to_assets - equity_to_assets 4 quarter-ends earlier (same cert)` | delta_ratio | -1 | P2 | Change in equity_to_assets over the last 4 quarters; NaN when the earlier quarter-end was not reported. |
| `d1q_tier1_leverage` | `tier1_leverage - tier1_leverage 1 quarter-end earlier (same cert)` | delta_percent | -1 | P2 | Change in tier1_leverage over the last 1 quarter; NaN when the earlier quarter-end was not reported. |
| `d4q_tier1_leverage` | `tier1_leverage - tier1_leverage 4 quarter-ends earlier (same cert)` | delta_percent | -1 | P2 | Change in tier1_leverage over the last 4 quarters; NaN when the earlier quarter-end was not reported. |
| `d1q_brokered_share` | `brokered_share - brokered_share 1 quarter-end earlier (same cert)` | delta_ratio | +1 | P2 | Change in brokered_share over the last 1 quarter; NaN when the earlier quarter-end was not reported. |
| `d4q_brokered_share` | `brokered_share - brokered_share 4 quarter-ends earlier (same cert)` | delta_ratio | +1 | P2 | Change in brokered_share over the last 4 quarters; NaN when the earlier quarter-end was not reported. |
| `d1q_uninsured_share` | `uninsured_share - uninsured_share 1 quarter-end earlier (same cert)` | delta_ratio | +1 | P2 | Change in uninsured_share over the last 1 quarter; NaN when the earlier quarter-end was not reported. |
| `d4q_uninsured_share` | `uninsured_share - uninsured_share 4 quarter-ends earlier (same cert)` | delta_ratio | +1 | P2 | Change in uninsured_share over the last 4 quarters; NaN when the earlier quarter-end was not reported. |
| `d1q_unrealized_loss_to_tier1` | `unrealized_loss_to_tier1 - unrealized_loss_to_tier1 1 quarter-end earlier (same cert)` | delta_ratio | -1 | P2 | Change in unrealized_loss_to_tier1 over the last 1 quarter; NaN when the earlier quarter-end was not reported. |
| `d4q_unrealized_loss_to_tier1` | `unrealized_loss_to_tier1 - unrealized_loss_to_tier1 4 quarter-ends earlier (same cert)` | delta_ratio | -1 | P2 | Change in unrealized_loss_to_tier1 over the last 4 quarters; NaN when the earlier quarter-end was not reported. |
| `neg_roa_quarters_last_8` | `count(roa_q < 0) over the last 8 quarter-ends incl. current` | count | +1 | P2 | Number of loss-making quarters among the last eight quarter-ends (current included), counted over the quarters the bank actually reported; NaN when fewer than four of the eight are available. Persistent losses erode capital. |
| `consecutive_loss_quarters` | `run length of roa_q < 0 ending at the current quarter (consecutive quarter-ends)` | count | +1 | P2 | How many quarters in a row, ending now, the bank has lost money; a missing quarter or a missing ROA breaks the run and a profitable quarter resets it to 0. |
| `noncurrent_rising_quarters_last_4` | `count(d1q_noncurrent_ratio > 0) over the last 4 quarter-ends incl. current` | count | +1 | P2 | Number of the last four quarter-ends (current included) at which the noncurrent ratio rose against the previous quarter; NaN when none of the four one-quarter changes is available. A steady climb precedes charge-offs. |

## Structure

17 features.

| name | formula | unit | sign | proto | explanation |
|---|---|---|---|---|---|
| `log_assets` | `log(asset)` | log_thousands_usd | -1 | P1 | Natural log of total assets in thousands of dollars; bank size on a scale where a doubling is a constant step. |
| `bank_age_years` | `(repdte - estymd) / 365.25 days` | years | -1 | P1 | Years since the charter was established; young (de novo) banks fail more often. |
| `has_holding_company` | `rssdhcr present` | flag | -1 | P1 | True when the bank sits under a holding company that can inject capital. |
| `bkclass_N` | `bkclass == 'N'` | flag | 0 | P1 | True when the charter class is N, that is a national bank supervised by the OCC. |
| `bkclass_NM` | `bkclass == 'NM'` | flag | 0 | P1 | True when the charter class is NM, that is a state-chartered bank that is not a Federal Reserve member. |
| `bkclass_SM` | `bkclass == 'SM'` | flag | 0 | P1 | True when the charter class is SM, that is a state-chartered bank that is a Federal Reserve member. |
| `bkclass_SB` | `bkclass == 'SB'` | flag | 0 | P1 | True when the charter class is SB, that is a savings bank. |
| `bkclass_SI` | `bkclass == 'SI'` | flag | 0 | P1 | True when the charter class is SI, that is a stock savings institution. |
| `bkclass_SL` | `bkclass == 'SL'` | flag | 0 | P1 | True when the charter class is SL, that is a savings and loan association. |
| `bkclass_NC` | `bkclass == 'NC'` | flag | 0 | P1 | True when the charter class is NC, that is a non-insured commercial bank. |
| `bkclass_OI` | `bkclass == 'OI'` | flag | 0 | P1 | True when the charter class is OI, that is another kind of insured institution. |
| `region_northeast` | `stalp in Census northeast region` | flag | 0 | P2 | One-hot indicator for a head office in the Census Bureau northeast region. |
| `region_midwest` | `stalp in Census midwest region` | flag | 0 | P2 | One-hot indicator for a head office in the Census Bureau midwest region. |
| `region_south` | `stalp in Census south region` | flag | 0 | P2 | One-hot indicator for a head office in the Census Bureau south region. |
| `region_west` | `stalp in Census west region` | flag | 0 | P2 | One-hot indicator for a head office in the Census Bureau west region. |
| `region_other` | `stalp not a state or DC` | flag | 0 | P2 | One-hot indicator for a head office in a territory or other non-state code. |
| `is_community_bank` | `cb` | flag | 0 | P2 | True when the FDIC designates the bank a community bank (local funding and lending footprint); such banks fail through credit, not through market runs. |

## Macro environment

6 features.

| name | formula | unit | sign | proto | explanation |
|---|---|---|---|---|---|
| `macro_unemp_rate` | `state unemployment rate (<ST>UR) as of avail_date` | percent | +1 | P2 | Seasonally adjusted unemployment rate of the head-office state, latest month published 45 days or more before the Call Report became available. Job losses turn into missed loan payments, so a higher level means a weaker local book. |
| `macro_unemp_change_4q` | `unemp_rate - unemp_rate 12 months earlier` | percentage_points | +1 | P2 | Twelve-month change in the state unemployment rate. A rising rate marks a local downturn that has not yet reached the loan book, so the change adds lead time over the level. |
| `macro_hpi_change_4q` | `log(<ST>STHPI / value four quarters earlier)` | log_ratio | -1 | P2 | Four-quarter log change in the FHFA all-transactions house price index of the state. Falling prices erode the collateral behind mortgage and construction loans and turn delinquencies into losses, so risk falls as prices rise. |
| `macro_t10y3m` | `T10Y3M (10-year minus 3-month Treasury) as of avail_date` | percentage_points | -1 | P2 | Slope of the Treasury curve. An inverted curve (negative spread) has preceded every recent recession and squeezes banks that fund long assets with short deposits, so a steeper curve means lower risk. |
| `macro_dgs10` | `DGS10 (10-year Treasury yield) as of avail_date` | percent | 0 | P2 | Level of the 10-year Treasury yield. A high level raises funding costs but also the yield on new loans, and low levels came with both the calm 2010s and the 2008 crisis, so no direction is imposed. |
| `macro_fedfunds_change_4q` | `FEDFUNDS - FEDFUNDS 12 months earlier` | percentage_points | +1 | P2 | Twelve-month change in the effective federal funds rate. A fast tightening cycle marks down fixed-rate securities and drains deposits to money funds (the 2023 mechanism), so risk rises with the change. |
